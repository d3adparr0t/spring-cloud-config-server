server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: "https://github.com/d3adparr0t/config-repo.git"
          clone-on-start: true
security:
  user:
    name: "root"
    password: "s3cr3t"